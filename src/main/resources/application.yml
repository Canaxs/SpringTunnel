spring:
  application:
    name: springtunnel

  datasource:
    url: jdbc:mysql://localhost:3306/tunnel
    username: "root"
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update